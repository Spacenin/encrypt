#include <iostream>
#include <fstream>
#include <string>
#include "cipher.h"

int cipher = 1;

std::string encrypt(std::string plaintext) {
    std::string ciphertext;
    switch(cipher) {
        case 1:
        {
            
        }
    }
}